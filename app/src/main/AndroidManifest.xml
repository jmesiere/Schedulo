<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exosethi.schedulo" >

    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--test-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".common.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".common.Inscription"
            android:label="@string/inscription" />
        <activity
            android:name=".student.StudentHomePage"
            android:label="@string/acceuil" />
        <activity
            android:name=".teacher.TeacherHomePage"
            android:label="@string/acceuil" />
        <activity
            android:name=".student.ConsultationNote"
            android:label="@string/consulterNote" />
        <activity
            android:name=".student.ConsultTeachers"
            android:label="@string/teacher"/>
        <activity
            android:name=".common.ConsultPlanning"
            android:label="@string/planning"/>
        <activity
            android:name=".student.ConsultCourse"
            android:label="@string/course"/>
    </application>

</manifest>
